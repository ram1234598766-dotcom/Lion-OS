[package]
name = "user"
version = "0.1.0"
edition = "2021"

# Standalone (a loadable program, not a workspace member of the kernel).
[workspace]

[dependencies]

[[bin]]
name = "user"
path = "src/main.rs"
test = false
bench = false

[profile.dev]
panic = "abort"
[profile.release]
panic = "abort"