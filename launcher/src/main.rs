//! `lionos` — host-side launcher CLI (Week-1 stub).
//!
//! Real subcommands (`run`, `doctor`, `update`) are scaffolded in Month 1
//! Week 2. This week it only exists so the launcher crate compiles for std.

fn main() {
    println!("lionos: LionOS launcher (Week-1 stub — full CLI lands in Month 1 Week 2)");
}
